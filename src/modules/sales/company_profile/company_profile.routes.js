"use strict"; const express=require("express");const {authMiddleware}=require("../../../middleware/auth");const {requirePermission}=require("../../../middleware/rbac");const c=require("./company_profile.controller");const v=require("./company_profile.validator");const router=express.Router();router.use(authMiddleware);router.get("/",requirePermission("MOD-23","view"),c.get);router.put("/",requirePermission("MOD-23","edit"),v.update,c.update);router.post("/refresh",requirePermission("MOD-23","edit"),c.refresh);module.exports={basePath:"/company-profile",feature:null,router};
