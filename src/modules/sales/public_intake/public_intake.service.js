"use strict";const quote=require("../quote_request/quote_request.service");const enquiry=require("../inbound_intake/inbound_intake.service");const partnership=require("../partnership_request/partnership_request.service");const campaign=require("../marketing_campaign/marketing_campaign.service");const receipt=row=>({received:true,reference:row.public_ref||row.quote_request_id||row.contact_enquiry_id||row.partnership_request_id||row.email});async function submitQuote(c,data){const row=await quote.create(c,{data:{...data,intake_channel:"WEBSITE"},actor:{}});return receipt(row);}async function submitContact(c,data){const row=await enquiry.submitEnquiry(c,{data:{...data,source:"WEBSITE"},actor:{}});return receipt(row);}async function submitPartnership(c,data){const row=await partnership.create(c,{data:{...data,intake_channel:"WEBSITE"},actor:{}});return receipt(row);}async function subscribe(c,data){const row=await campaign.subscribe(c,{email:data.email,name:data.name,source:"website",actor:{}});return receipt(row);}module.exports={submitQuote,submitContact,submitPartnership,subscribe,receipt};
