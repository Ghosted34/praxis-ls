# Moved → `doc/FRONTEND_GUIDE.md`

This file was renamed in Phase 5 of `doc/DESKTOP_UI_AUDIT.md`, which consolidates the
frontend documentation into one guide (F15).

**Nothing was lost.** `FRONTEND_GUIDE.md` *is* this document — same content, renamed, plus
the density and desktop-interaction sections Phase 5 added and a table naming every older
frontend plan it supersedes.

This stub exists because the audit and `SESSION_HISTORY.md` cite this path by name, and they
are historical records: they are describing the file as it was, and rewriting them to point
somewhere else would falsify the record. A reader following one of those citations should
land here and be sent on, rather than at a missing file.

Do not add content here.
