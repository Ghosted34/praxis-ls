node_modules/
dist/
build/
coverage/
logs/
media/
uploads/
*.min.js
package-lock.json

# Generated compactly from the official 3,446-code GLEIF CSV. Reformatting this
# array adds thousands of lines without making the source more maintainable.
packages/shared/data/legal-forms.generated.js

# Praxis backend sources are hand-formatted; skip to avoid a sync/truncation race
src/
scripts/

# GENERATED by scripts/generate-api-docs.js, and byte-compared by the "API docs
# in sync" CI gate. Prettier re-aligns the markdown tables, which changes the
# bytes without changing the content — so `npm run format` alone was enough to
# fail CI with a DRIFT error on a branch that touched no code, and to turn every
# subsequent merge of these files into a conflict (the whole table reflows).
# Regenerate them; never format them.
doc/API_REFERENCE.md
doc/ERROR_CODES.md
