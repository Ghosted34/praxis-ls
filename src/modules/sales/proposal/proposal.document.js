"use strict";
const esc=v=>String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));
function html({proposal,lines=[],narratives=[],client=null}){const money=n=>new Intl.NumberFormat("fr-FR",{style:"currency",currency:proposal.currency||"XAF",maximumFractionDigits:0}).format(Number(n)||0);const body=narratives.map(n=>`<section lang="${esc(n.language)}"><h2>${esc(n.section)}</h2><p>${esc(n.body)}</p></section>`).join("");const rows=lines.map(l=>`<tr><td>${esc(l.label)}</td><td>${esc(l.qty)}</td><td>${esc(money(l.unit_price))}</td><td>${esc(money(Number(l.qty)*Number(l.unit_price)))}</td></tr>`).join("");return `<!doctype html><html><head><meta charset="utf-8"><style>body{font-family:Roboto,'Noto Sans',sans-serif;color:#172033;margin:32px}header{border-bottom:2px solid #172033}table{width:100%;border-collapse:collapse}td,th{padding:8px;border-bottom:1px solid #ddd;text-align:left}@page{size:A4;margin:18mm}</style></head><body><header><h1>${esc(proposal.title)}</h1><p>${esc(client?.legal_name||client?.name||"")} · ${esc(proposal.doc_number||"")}</p></header><p>${esc(proposal.origin_location||"")} → ${esc(proposal.destination_location||"")}</p>${body}<table><thead><tr><th>Service</th><th>Qty</th><th>Unit</th><th>Total</th></tr></thead><tbody>${rows}</tbody></table></body></html>`;}
module.exports={html,esc};
